<%- include('../partials/header') %>

<table>
    <thead>
        <tr>
            <th>Trail</th>
            <th>Dist (mi)</th>
            <th>Average<br>Rating</th>
        </tr>
    </thead>
    <tbody>
        <% trails.forEach(function(t) { %>
            <tr>
                <td><a href="/trails/<%= t._id %>"><%= t.name %></a></td>
                <td><%= t.distance %></td>
                <td>[Placeholder Rating]</td>
            </tr>
        <% }); %>
    </tbody>
</table>

<%- include('../partials/footer') %>